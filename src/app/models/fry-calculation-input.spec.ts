import { FryCalculationInput } from './fry-calculation-input';

describe('FryCalculationInput', () => {
  it('should create an instance', () => {
    expect(new FryCalculationInput()).toBeTruthy();
  });
});
